version: 0.2

phases:
  install:
    commands:
      - echo Nothing to do in the pre_build phase...
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - npm build
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - \hello-world\app.js
